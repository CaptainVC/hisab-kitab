# Dashboard notes

- EXPENSE totals/charts should count only rows with `type === 'EXPENSE'`.
- Pass-through spends for someone else should be recorded as `type === 'SPLITWISE'` using `(sw-Name)` shorthand in hisab input.
