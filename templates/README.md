# Templates

- `HK_TEMPLATE.pretty.xlsx` — empty, formatted workbook template (Transactions sheet)

Generate/update with:

```bash
node src/excel/build_template.js --out templates/HK_TEMPLATE.pretty.xlsx
```
