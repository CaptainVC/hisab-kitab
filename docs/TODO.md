# Hisab Kitab — TODO

## Testing
- [ ] Add **unit tests** (core parsing + categorization + refs logic + API route handlers)
- [ ] Add **e2e tests**
  - Option A: **Playwright** (recommended for the web UI + API flows)
  - Option B: **Reqnroll** (BDD-style) for end-to-end scenarios

## Web app follow-ups
- [ ] Harden auth/session (expiry, 401 → redirect to login)
- [ ] Tailscale deployment: install Tailscale + bind server to tailscale IP
